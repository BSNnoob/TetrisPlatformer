using UnityEngine;

// Marker component used to identify player protection blocks created at runtime
public class PlayerProtectionBlock : MonoBehaviour
{
    // Intentionally empty; serves only as an identifier for cleanup
}